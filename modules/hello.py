async def hello_handler(update: dict) -> str:
    """
    Обработчик команды /hello.
    Возвращает приветственное сообщение.

    🔍 Извлекает user_id, но не отображает его по умолчанию.
    Это позволяет использовать ID для логики, но не засорять интерфейс.
    """
    user_id = update.get("user_id")  # доступен для логики, но не выводится
    return "Привет! Я — BotKit 🤖"